import 'package:flutter/material.dart';

class FormLargeField extends StatelessWidget {
  const FormLargeField({super.key, required this.child});

  final Widget child;

  @override
  Widget build(BuildContext context) {
    return child;
  }
}
